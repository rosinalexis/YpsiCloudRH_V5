version: "3.7"
services:
  database:
    image: "mysql:latest"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: ypsi_cloud_rh_v5
    ports:
      - 60561:3306
    networks:
      - dev

  phpmyadmin:
    image: phpmyadmin
    container_name: phpmyadmin_docker_symfony
    restart: always
    depends_on:
      - database
    ports:
      - 8082:80
    environment:
      PMA_HOST: database
    networks:
      - dev

networks:
  dev:
